# AGENTS.md — MascotasPerdidas

## Auto commit + push authorization

Authorized to auto `git commit` + `git push` after each green phase per Fase 2 plan (`docs/WORKPLAN.md`).
Authorized by user on 2026-06-02.

## Rules

- Never commit `google-services.json`.
- Never print tokens, UIDs, or personal data in logs.
- Compile + detekt must be green before each commit.
