package com.mascotasperdidas.app.app.theme

import androidx.compose.ui.graphics.Color

// ── Token primario (violeta M3) ──────────────────────────────────
val Primary = Color(0xFF6750A4)
val OnPrimary = Color(0xFFFFFFFF)
val PrimaryContainer = Color(0xFFEADDFF)
val OnPrimaryContainer = Color(0xFF21005D)

// ── Token secundario (amarillo suave — chip RECIENTE) ────────────
val Secondary = Color(0xFFF6E27A)
val OnSecondary = Color(0xFF3A3200)

// ── Token terciario (verde — chip HALLAZGO / botón CONTACTAR) ────
val Tertiary = Color(0xFF2E7D32)
val OnTertiary = Color(0xFFFFFFFF)

// ── Error (rojo coral — chip PERDIDO / eliminar cuenta) ────────
val Error = Color(0xFFE57373)
val OnError = Color(0xFFFFFFFF)

// ── Token "care" (naranja — bajo mi cuidado) ─────────────────────
val Care = androidx.compose.ui.graphics.Color(0xFFE65100)
val CareContainer = androidx.compose.ui.graphics.Color(0xFFFFE0B2)
val OnCare = androidx.compose.ui.graphics.Color(0xFFFFFFFF)
val OnCareContainer = androidx.compose.ui.graphics.Color(0xFF4E2600)

// ── Superficies ──────────────────────────────────────────────────
val Background = Color(0xFFF3E8F7)   // lila muy claro — fondo app
val OnBackground = Color(0xFF1C1B1F)
val Surface = Color(0xFFFFFBFE)      // blanco — cards
val OnSurface = Color(0xFF1C1B1F)
val SurfaceVariant = Color(0xFFE7E0EC)
val OnSurfaceVariant = Color(0xFF49454F)
